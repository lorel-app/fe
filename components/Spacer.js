import React from "react";
import { View } from "react-native";

const Spacer = () => (
  <View
    style={{
      height: 5,
      width: "100%",
      opacity: 0,
    }}
  />
);

export default Spacer;
